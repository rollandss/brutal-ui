# 📱 Responsive Features & Effects

## Зміни в дизайні

### ✅ Адаптивність (Responsive Design)

Всі секції тепер повністю адаптивні для мобільних пристроїв:

- **Header**

  - Масштабується від `text-4xl` на мобільних до `text-8xl` на десктопі
  - Тіні змінюються від `4px` до `8px` залежно від розміру екрану
  - Кнопки стають full-width на мобільних, inline на більших екранах

- **Grid Layouts**

  - Cards: `grid-cols-1` → `sm:grid-cols-2` → `lg:grid-cols-3`
  - Stats: `grid-cols-1` → `sm:grid-cols-2` → `lg:grid-cols-4`
  - Features: `grid-cols-1` → `md:grid-cols-2`

- **Spacing**

  - Padding: `p-4` на мобільних → `sm:p-6` на планшетах
  - Gaps: `gap-3` → `sm:gap-4` → `lg:gap-6`
  - Margins: `space-y-8` → `lg:space-y-12`

- **Borders & Shadows**
  - Borders: `border-3` → `sm:border-4`
  - Shadows: `shadow-[4px_4px_0px_0px]` → `sm:shadow-[8px_8px_0px_0px]`

### 🎨 Нові Компоненти

1. **Statistics Cards (📊)**

   - 4 метрики з градієнтними фонами
   - Hover effect: `translateY(-4px)` при наведенні
   - Показують: 39+ компонентів, 100% TypeScript, швидкість, brutal стиль

2. **Feature Highlights (✨)**

   - 4 карточки з основними перевагами
   - Group hover з масштабуванням іконок (`scale-110`)
   - Збільшення тіні при hover до `12px`
   - Опис: Next.js 16, Brutalism First, Accessible, Responsive

3. **Development Timeline (🗓️)**

   - Візуальна хронологія розробки з 4 етапів
   - Кольорові кола з іконками (✓, 2, 3, 4)
   - Вертикальна лінія з'єднує етапи
   - Badges показують статус: Completed, In Progress, Active, Coming Soon
   - Group hover на кожному кроці з `scale-110` на колі

4. **Newsletter/CTA Section (🚀)**
   - Яскравий градієнтний фон (yellow → orange → pink)
   - Email input + CTA кнопка
   - Адаптивний layout: вертикальний на мобільних, горизонтальний на десктопі
   - Великі тіні (`12px` на десктопі)

### ⚡ Нові Анімації та Ефекти

#### Анімації в globals.css:

```css
@keyframes pulse - пульсація (opacity + scale)
@keyframes wiggle - хитання (-3deg → 3deg)
@keyframes float - плавання вгору-вниз (10px)
@keyframes glow - світіння тіні (5px → 20px);
```

#### Utility Classes:

- `.animate-pulse` - безкінечна пульсація (2s)
- `.animate-wiggle` - хитання (1s infinite)
- `.animate-float` - плавання (3s infinite)
- `.animate-glow` - світіння (2s infinite)

#### Hover Effects:

- `.hover-lift:hover` - підйом вгору на 4px
- `.hover-grow:hover` - збільшення на 5% (scale 1.05)
- `.hover-rotate:hover` - поворот на 5 градусів

#### Застосовані effects:

- **Statistics cards**: `hover:translate-y-[-4px]` + transition
- **Feature cards**: `group-hover:scale-110` на іконках + `hover:shadow-[12px_12px_0px]`
- **Timeline steps**: `group-hover:scale-110` на кружечках
- **Footer links**: `hover:translate-y-[-2px]` на кнопках

### 🎯 Покращення UX

1. **Mobile-First підхід**

   - Всі розміри починаються з мобільних
   - Прогресивне покращення для більших екранів

2. **Touch-friendly**

   - Більші кнопки на мобільних (full-width)
   - Достатні відступи для пальців (min 44px)

3. **Readable typography**

   - Масштабовані розміри шрифтів
   - Від `text-xs` до `text-5xl` залежно від екрану

4. **Visual hierarchy**
   - Чіткі заголовки з borders
   - Різні розміри тіней для акцентів
   - Кольорові градієнти для важливих блоків

## Використані breakpoints

- **sm**: 640px - планшети portrait
- **md**: 768px - планшети landscape
- **lg**: 1024px - десктоп
- **xl**: 1280px - великі монітори (не використовується поки)

## Тестування

Перевірте сайт на різних розмірах:

```bash
npm run dev
```

Відкрийте http://localhost:3000 та змініть розмір вікна або використовуйте DevTools для емуляції мобільних пристроїв.

## Deployments

Оновлена версія працює на:

- Vercel (виробництво)
- GitHub Pages (з basePath='/brutal-ui')

Всі зміни адаптовані для обох платформ завдяки умові:

```js
basePath: process.env.GITHUB_ACTIONS ? '/brutal-ui' : '';
```
